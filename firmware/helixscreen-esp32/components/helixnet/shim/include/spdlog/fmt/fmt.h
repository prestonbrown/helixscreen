// SPDX-License-Identifier: GPL-3.0-or-later
// Alias: <spdlog/fmt/fmt.h> → the repo's bundled fmt (header-only), routed
// through the shim so FMT_HEADER_ONLY stays configured in one place.
#pragma once
#include "../../../spdlog_shim.h"
